# Upstream

Dataset and BM25 search design vendored from [genjutsu](https://github.com/AThevon/genjutsu) `ui-ux-pro-max` (itself based on nextlevelbuilder/ui-ux-pro-max-skill, MIT).

Amaterasu changes:
- Claude packaging / host probes removed
- Python CLI replaced by Go (`cli/`)
- Markdown-only output
- Renamed surface: amaterasu

CSV files under `data/` are otherwise unchanged from the upstream snapshot in `drafts/genjutsu`.
